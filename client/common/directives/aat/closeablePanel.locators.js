"use strict";

var reviewAssistanceLocators = function() {
  this.acceptButton = by.id('acceptAssistanceBtn');
  this.closeButton = by.id('closeable-panel-close-btn');
};

module.exports = reviewAssistanceLocators;
